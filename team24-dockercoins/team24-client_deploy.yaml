apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: team24-client
  name: team24-client
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: team24-client
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: team24-client
    spec:
      containers:
      - image: arifirst/team24-client:v1
        imagePullPolicy: IfNotPresent
        name: team24-client
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      terminationGracePeriodSeconds: 30
