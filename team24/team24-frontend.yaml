apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: frontend
  namespace: default
spec:
  template:
    spec:
      containers:
        - image: docker.io/toeikanta/flight-booking-frontend:v0.4
          env:
            - name: TARGET
              value: "Go Sample v1"
